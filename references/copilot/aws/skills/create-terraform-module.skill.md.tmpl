---
name: create-terraform-module
description: "Create a new reusable AWS Terraform module following {{COMPANY_NAME}} conventions. Use when: scaffolding a new {{MODULE_PREFIX}}-* repo, adding AWS resource modules, creating module boilerplate."
---

# Create Terraform Module — AWS

Scaffolds a new `{{MODULE_PREFIX}}-{name}` directory following workspace conventions.

## When to Use
- Creating a new AWS resource module from scratch
- Generating boilerplate for a new module repo
- Adding standard files (tests, examples, pre-commit) to an existing module

## Prerequisites
- Module name (lowercase, hyphenated)
- Primary AWS resource type(s)
- Optional features needed (VPC endpoints, KMS encryption, IAM roles)

## Procedure

### 1. Gather Requirements
Ask for:
- Module name: used as `{{MODULE_PREFIX}}-{name}`
- Primary resource(s) to manage
- Optional features: {{OPTIONAL_FEATURES}}
<!-- Example AWS features: vpc_endpoints, kms_encryption, iam_role, cloudwatch_logs -->

### 2. Create Directory Structure
```
{{MODULE_PREFIX}}-{name}/
├── main.tf                    # Provider requirements + data sources
├── {resource}.tf              # Core resource(s)
├── locals.tf                  # Name construction, tag merging
├── variables.tf               # Module-specific variables
├── {{COMMON_VARS_FILE}}       # Standard cross-module variables
├── outputs.tf                 # Module outputs
├── versions.tf                # Version constraints
├── README.md                  # Documentation
├── .pre-commit-config.yaml    # Validation hooks
├── .tflint.hcl                # TFLint rules
├── .terraform-docs.yml        # Doc generation
├── examples/
│   └── basic/
│       ├── main.tf
│       └── variables.tf
└── tests/
    └── {resource}.tftest.hcl  # Native terraform tests
```

### 3. File Templates

#### versions.tf
```hcl
terraform {
  required_version = ">=1.3"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">=5.0,<6.0"
    }
  }
}
```

#### locals.tf — Naming Pattern
```hcl
locals {
  {{NAMING_LOCALS}}
  # Tags: defaults + resource-specific, resource-specific wins
  tags = merge(var.env_default_tags, var.tags)
}
```

#### common.variables.tf — Standard Cross-Module Variables
```hcl
variable "prefix" {
  type        = string
  description = "Resource name prefix."
}

variable "region" {
  type        = string
  description = "AWS region."
}

variable "tags" {
  type        = map(string)
  default     = {}
  description = "Additional resource tags. Merged with env_default_tags (these win on conflict)."
}

variable "env_default_tags" {
  type        = map(string)
  default     = {}
  description = "Default tags from the orchestration layer."
}
```

#### Resource file pattern
```hcl
resource "{{PROVIDER_RESOURCE}}" "default" {
  # name or name_prefix depends on the resource type
  {{NAMING_ATTRIBUTE}}

  tags = local.tags

  # Resource-specific configuration...
}
```

#### outputs.tf
```hcl
output "name" {
  value       = {{PROVIDER_RESOURCE}}.default.{{NAME_ATTRIBUTE}}
  description = "Name of the resource."
}

output "id" {
  value       = {{PROVIDER_RESOURCE}}.default.id
  description = "The ID of the resource."
}

output "arn" {
  value       = {{PROVIDER_RESOURCE}}.default.arn
  description = "The ARN of the resource."
}
```

#### Test file pattern
```hcl
mock_provider "aws" {}

override_data {
  target = data.aws_caller_identity.current
  values = {
    account_id = "123456789012"
    arn        = "arn:aws:iam::123456789012:root"
    user_id    = "123456789012"
  }
}

variables {
  prefix           = "test-auto"
  region           = "us-east-1"
  tags             = {}
  env_default_tags = { managed_by = "Terraform" }
}

run "creates_resource_with_correct_name" {
  command = plan
  assert {
    condition     = {{PROVIDER_RESOURCE}}.default.{{NAME_ATTRIBUTE}} == "{{EXPECTED_NAME_PATTERN}}"
    error_message = "Name should follow naming convention"
  }
}

run "merges_tags_correctly" {
  command = plan
  variables {
    tags = { extra = "tag" }
  }
  assert {
    condition     = {{PROVIDER_RESOURCE}}.default.tags["extra"] == "tag"
    error_message = "Custom tags should be merged"
  }
}
```

### 4. Post-Creation
1. `terraform fmt -recursive`
2. `terraform validate`
3. `terraform test`
4. `terraform-docs` to generate README
